/*!
 * plotly.js (plotly.min.js)
 * https://plotly.com/javascript/
 *
 * Copyright (c) Plotly Technologies Inc.
 * Released under the MIT License:
 * https://github.com/plotly/plotly.js/blob/master/LICENSE
 *
 * NOTE: plotly.min.js itself is NOT checked into git. It is copied verbatim
 * from the installed `plotly` pip package (plotly/package_data/plotly.min.js)
 * at Docker build time and again — idempotently — by the app lifespan
 * (plot_utils.ensure_plotly_js_asset), so the served JS always matches the
 * python package version that generated the chart HTML.
 */
